apiVersion: v1
kind: Pod
metadata:
  name: test-bad-root-user
  namespace: demo
  labels:
    app: test-root-user
spec:
  securityContext:
    runAsUser: 0
  containers:
  - name: web
    image: nginx:1.25.1
    resources:
      limits:
        cpu: 100m
        memory: 64Mi

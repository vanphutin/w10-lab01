apiVersion: v1
kind: Pod
metadata:
  name: test-bad-host-network
  namespace: demo
  labels:
    app: test-host-network
spec:
  hostNetwork: true
  containers:
  - name: web
    image: nginx:1.25.1
    resources:
      limits:
        cpu: 100m
        memory: 64Mi

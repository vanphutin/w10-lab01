apiVersion: v1
kind: Pod
metadata:
  name: test-good-pod
  namespace: demo
  labels:
    app: test-good
spec:
  securityContext:
    runAsUser: 1000
  containers:
  - name: web
    image: nginx:1.25.1
    resources:
      limits:
        cpu: 100m
        memory: 64Mi

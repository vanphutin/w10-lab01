apiVersion: v1
kind: Pod
metadata:
  name: test-bad-latest-tag
  namespace: demo
  labels:
    app: test-latest
spec:
  containers:
  - name: web
    image: nginx:latest
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
